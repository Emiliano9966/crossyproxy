{
  "version": 2,
  "builds": [
    { "src": "api/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/proxy", "dest": "/api/proxy.ts" },
    { "src": "/resource", "dest": "/api/resource.ts" }
  ]
}
